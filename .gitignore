node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files
.env.local
.env
.env.*.local

# Local directories
.local/
.claude/

# Temporary files
nul

# File scritto a runtime dal server dev con la porta effettiva (vedi vite.config.ts)
.dev-port

# Dati reali dell'applicazione (clienti, fatture, utenti, password hash)
# NON vanno mai committati, neppure in un repo privato.
data/
uploads/
attached_assets/

# Automatic snapshot backups created on server startup
backups/
data-backup-*/
